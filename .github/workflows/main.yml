name: MainChecker
on: push
jobs:
  run-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: CheckOut
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: 21
          distribution: 'adopt'

      - name: Unit Tests
        run: mvn clean package

      - name: "TODO to Issue"
        with:
          PROJECTS_SECRET: ${{ secrets.SECRET_SCIPARO_KEY}}
        uses: "alstr/todo-to-issue-action@v4"




